import { render, screen } from '@testing-library/react';
import Header from './Header';

describe("Header", () => {
    it('should render the character name passed via the name prop', () => {

    });
})